import {
  createMiddleware,
  createServer
} from "./chunk-SPL6Q7DV.js";

// src/native-fetch.ts
import * as webStream from "stream/web";
import installGetSetCookie from "@hattip/polyfills/get-set-cookie";
installGetSetCookie();
for (const key of Object.keys(webStream)) {
  if (!(key in global)) {
    global[key] = webStream[key];
  }
}
export {
  createMiddleware,
  createServer
};
